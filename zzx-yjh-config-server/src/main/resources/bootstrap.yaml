server:
  port: 8767
spring:
  application:
    name: fangjia-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/forezp/SpringcloudConfig/
          username:
          passphrase:
          search-paths: respo
      label: master
